import { primaryTextColors, secondaryTextColor } from "./colors";

export const headerText = `text-2xl font-extrabold leading-tight ${primaryTextColors}`
export const sectionHeader = `text-4xl font-extrabold text-center py-5 ${primaryTextColors}`
export const sectionSubheader = `text-xl font-bold text-center ${secondaryTextColor}`

export const smallText = `${primaryTextColors} text-sm`
export const largeText = `${primaryTextColors} text-lg`
export const extraLargeText = `${primaryTextColors} text-xl`